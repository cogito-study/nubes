# Migration `20200102200639-create-new-major-request`

This migration has been generated by kiralybalint at 1/2/2020, 8:06:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."NewMajorRequest" (
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "deletedAt" timestamp(3)   ,
  "faculty" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "institute" text NOT NULL DEFAULT '' ,
  "major" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "user" text NOT NULL  REFERENCES "public"."User"("id") ON DELETE RESTRICT,
  PRIMARY KEY ("id")
);

CREATE UNIQUE INDEX "NewMajorRequest.id" ON "public"."NewMajorRequest"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191211155200-language-code-enum..20200102200639-create-new-major-request
--- datamodel.dml
+++ datamodel.dml
@@ -1,6 +1,6 @@
 datasource postgres {
-  url = "***"
+  url      = env("POSTGRESQL_URL")
   provider = "postgres"
 }
 generator photon {
@@ -78,8 +78,19 @@
   updatedAt   DateTime          @updatedAt
   deletedAt   DateTime?
 }
+model NewMajorRequest {
+  id        String    @default(cuid()) @id @unique
+  institute String
+  major     String
+  faculty   String
+  user      User      @relation(name: "UserNewMajorRequest")
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  deletedAt DateTime?
+}
+
 model Note {
   id             String              @default(cuid()) @id @unique
   content        String
   contentHTML    String
@@ -224,8 +235,9 @@
   passwordToken       PasswordToken?      @relation(name: "PasswordTokenUser")
   departments         Department[]        @relation(name: "DepartmentLeader")
   institutes          Institute[]         @relation(name: "InstituteUsers")
   preferredLanguage   Language?           @relation(name: "UserPrefferedLanguage")
+  newMajorRequest     NewMajorRequest?    @relation(name: "UserNewMajorRequest")
   departmentPermissions         DepartmentPermission[]         @relation(name: "DepartmentPermissionUsers")
   facultyPermissions            FacultyPermission[]            @relation(name: "FacultyPermissionUsers")
   institutePermissions          InstitutePermission[]          @relation(name: "InstitutePermissionUsers")
```

## Photon Usage

You can use a specific Photon built for this migration (20200102200639-create-new-major-request)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20200102200639-create-new-major-request'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
