# Migration `20190911151330-add-language`

This migration has been generated by kiralybalint <balint.kiraly@cogito.study> at 9/11/2019, 3:13:30 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Language"("id" text NOT NULL  ,"code" text NOT NULL DEFAULT '' ,"name" text NOT NULL DEFAULT '' ,PRIMARY KEY ("id"))
;

ALTER TABLE "public"."NoteComment" DROP COLUMN "author",ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."NoteCommentThread" DROP COLUMN "note",ADD COLUMN "note" text   REFERENCES "public"."Note"("id") ON DELETE SET NULL;

ALTER TABLE "public"."PasswordToken" DROP COLUMN "user",ADD COLUMN "user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Department" DROP COLUMN "leader",ADD COLUMN "leader" text   REFERENCES "public"."User"("id") ON DELETE SET NULL,DROP COLUMN "institute",ADD COLUMN "institute" text   REFERENCES "public"."Institute"("id") ON DELETE SET NULL;

ALTER TABLE "public"."Suggestion" DROP COLUMN "note",ADD COLUMN "note" text   REFERENCES "public"."Note"("id") ON DELETE SET NULL,DROP COLUMN "author",ADD COLUMN "author" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."_SubjectTeachers" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Subject"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."_SubjectStudents" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Subject"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."_NoteAuthors" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Note"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."SubjectInformation" DROP COLUMN "subject",ADD COLUMN "subject" text   REFERENCES "public"."Subject"("id") ON DELETE SET NULL;

ALTER TABLE "public"."_InstituteUsers" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Institute"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."NoteHighlight" DROP COLUMN "note",ADD COLUMN "note" text   REFERENCES "public"."Note"("id") ON DELETE SET NULL,DROP COLUMN "user",ADD COLUMN "user" text   REFERENCES "public"."User"("id") ON DELETE SET NULL;

ALTER TABLE "public"."_SuggestionLikers" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Suggestion"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."Note" DROP COLUMN "subject",ADD COLUMN "subject" text   REFERENCES "public"."Subject"("id") ON DELETE SET NULL;

ALTER TABLE "public"."_CommentLikers" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."NoteComment"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."Subject" ADD COLUMN "language" text   REFERENCES "public"."Language"("id") ON DELETE SET NULL,DROP COLUMN "department",ADD COLUMN "department" text   REFERENCES "public"."Department"("id") ON DELETE SET NULL;

ALTER TABLE "public"."_NoteLikers" DROP COLUMN "A",ADD COLUMN "A" text   REFERENCES "public"."Note"("id") ON DELETE CASCADE,DROP COLUMN "B",ADD COLUMN "B" text   REFERENCES "public"."User"("id") ON DELETE CASCADE;

ALTER TABLE "public"."User" ADD COLUMN "preferredLanguage" text   REFERENCES "public"."Language"("id") ON DELETE SET NULL,DROP COLUMN "role",ADD COLUMN "role" text   REFERENCES "public"."UserRole"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "Language.id._UNIQUE" ON "public"."Language"("id")

CREATE UNIQUE INDEX "Language.code._UNIQUE" ON "public"."Language"("code")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190808153457-add-is-active-flag-to-user..20190911151330-add-language
--- datamodel.dml
+++ datamodel.dml
@@ -43,8 +43,16 @@
   updatedAt   DateTime     @updatedAt
   deletedAt   DateTime?
 }
+model Language {
+  id       String    @default(cuid()) @id @unique
+  code     String    @unique
+  name     String
+  users    User[]    @relation(name: "UserPrefferedLanguage")
+  subjects Subject[] @relation(name: "SubjectLanguage")
+}
+
 model Note {
   id             String              @default(cuid()) @id @unique
   content        String
   contentHTML    String
@@ -114,8 +122,9 @@
   teachers     User[]               @relation(name: "SubjectTeachers")
   students     User[]               @relation(name: "SubjectStudents")
   informations SubjectInformation[] @relation(name: "SubjectInformations")
   notes        Note[]               @relation(name: "SubjectNotes")
+  language     Language             @relation(name: "SubjectLanguage")
   createdAt    DateTime             @default(now())
   updatedAt    DateTime             @updatedAt
   deletedAt    DateTime?
 }
@@ -167,8 +176,9 @@
   likedSuggestions    Suggestion[]    @relation(name: "SuggestionLikers")
   passwordToken       PasswordToken?  @relation(name: "PasswordTokenUser")
   departments         Department[]    @relation(name: "DepartmentLeader")
   institutes          Institute[]     @relation(name: "InstituteUsers")
+  preferredLanguage   Language?      @relation(name: "UserPrefferedLanguage")
   createdAt           DateTime        @default(now())
   updatedAt           DateTime        @updatedAt
   deletedAt           DateTime?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190911151330-add-language)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190911151330-add-language'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
