# Migration `20191002161419-update-permissions-enum`

This migration has been generated by kiralybalint <balint.kiraly@cogito.study> at 10/2/2019, 4:14:19 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191002152800-init-auth..20191002161419-update-permissions-enum
--- datamodel.dml
+++ datamodel.dml
@@ -205,8 +205,9 @@
 }
 enum DepartmentPermissionType {
   UPDATE_DEPARTMENT
+  DELETE_DEPARTMENT
   CREATE_SUBJECT
 }
 model InstitutePermission {
@@ -219,8 +220,9 @@
 }
 enum InstitutePermissionType {
   UPDATE_INSTITUTE
+  DELETE_INSTITUTE
   CREATE_DEPARTMENT
 }
 model NoteCommentPermission {
@@ -232,10 +234,10 @@
   deletedAt DateTime?
 }
 enum NoteCommentPermissionType {
-  UPDATE
-  DELETE
+  UPDATE_NOTE_COMMENT
+  DELETE_NOTE_COMMENT
 }
 model NoteCommentThreadPermission {
   id        String                          @default(cuid()) @id @unique
@@ -246,10 +248,10 @@
   deletedAt DateTime?
 }
 enum NoteCommentThreadPermissionType {
-  UPDATE
-  DELETE
+  UPDATE_NOTE_COMMENT_THREAD
+  DELETE_NOTE_COMMENT_THREAD
 }
 model NoteHighlightPermission {
   id        String                      @default(cuid()) @id @unique
@@ -260,10 +262,10 @@
   deletedAt DateTime?
 }
 enum NoteHighlightPermissionType {
-  UPDATE
-  DELETE
+  UPDATE_NOTE_HIGHLIGHT
+  DELETE_NOTE_HIGHLIGHT
 }
 model NotePermission {
   id        String             @default(cuid()) @id @unique
@@ -307,8 +309,9 @@
 }
 enum SubjectPermissionType {
   UPDATE_SUBJECT
+  DELETE_SUBJECT
   CREATE_NOTE
 }
 model SubjectInformationPermission {
@@ -320,9 +323,10 @@
   deletedAt DateTime?
 }
 enum SubjectInformationPermissionType {
-  UPDATE
+  UPDATE_SUBJECT_INFORMATION
+  DELETE_SUBJECT_INFORMATION
 }
 model SuggestionPermission {
   id        String                   @default(cuid()) @id @unique
@@ -334,9 +338,9 @@
 }
 enum SuggestionPermissionType {
   UPDATE_SUGGESTION
-  CREATE_NOTE
+  DELETE_SUGGESTION
 }
 model UserPermission {
   id        String             @default(cuid()) @id @unique
@@ -348,8 +352,9 @@
 }
 enum UserPermissionType {
   UPDATE_USER
+  DELETE_USER
 }
 model UserGroup {
   id          String       @default(cuid()) @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20191002161419-update-permissions-enum)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191002161419-update-permissions-enum'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
